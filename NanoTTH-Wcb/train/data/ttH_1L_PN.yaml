treename: Events

selection:
   ### use `&`, `|`, `~` for logical operations on numpy arrays
   ### can use functions from `math`, `np` (numpy), and `awkward` in the expression
   (event%2==0)

test_time_selection: (event%2==1)

new_variables:
   ### [format] name: formula
   ### can use functions from `math`, `np` (numpy), and `awkward` in the expression
   ttHcc: genEventClassifier==0
   ttHbb: genEventClassifier==1
   ttHtautau: genEventClassifier==2
   ttLF: genEventClassifier==3
   ttcc: (genEventClassifier>=4) & (genEventClassifier<=6)
   ttbb: (genEventClassifier>=7) & (genEventClassifier<=9)
   ak4_mask: ak.ones_like(ak4_pt)
   ak4p4: _p4_from_ptetaphim(ak4_pt, ak4_eta, ak4_phi, ak4_mass)
   ak4_energy: ak4p4.energy
   ak4_deltaPhi: ak4_phi - ak4_phi[:, 0]
   ak4_pt_log: np.log(ak4_pt)
   ak4_energy_log: np.log(ak4_energy)
   lep_mask: ak.ones_like(lep1_pt)
   lepp4: _p4_from_ptetaphim(lep1_pt, lep1_eta, lep1_phi, lep1_mass)
   lep_energy: lepp4.energy
   lep_pt_log: np.log(lep1_pt)
   lep_energy_log: np.log(lep_energy)
   lep_isMu: np.abs(lep1_pdgId) == 13

preprocess:
   ### method: [manual, auto] - whether to use manually specified parameters for variable standardization
   method: manual
   ### data_fraction: fraction of events to use when calculating the mean/scale for the standardization
   data_fraction: 1

inputs:
   jet_points:
      length: 10
      pad_mode: wrap
      vars:
         - [ak4_eta, null]
         - [ak4_phi, null]
   jet_features:
      length: 10
      pad_mode: wrap
      vars:
         ### [format 1]: var_name (no transformation)
         ### [format 2]: [var_name,
         ###              subtract_by(optional, default=None, no transf. if preprocess.method=manual, auto transf. if preprocess.method=auto),
         ###              multiply_by(optional, default=1),
         ###              clip_min(optional, default=-5),
         ###              clip_max(optional, default=5),
         ###              pad_value(optional, default=0)]
         - [ak4_pt_log, 4, 1]
         - [ak4_energy_log, 4, 1]
         - [ak4_eta, null]
         - [ak4_deltaPhi, null]
         - [ak4_bdisc, null]
         - [ak4_cvldisc, null]
         - [ak4_cvbdisc, null]
   jet_mask:
      length: 10
      pad_mode: constant
      vars:
         - [ak4_mask, null]
   lep_points:
      length: 1
      vars:
         - [lep1_eta, null]
         - [lep1_phi, null]
   lep_features:
      length: 1
      vars:
         - [lep_pt_log, 4, 1]
         - [lep_energy_log, 4, 1]
         - [lep_isMu, null]
   lep_mask:
      length: 1
      vars:
         - [lep_mask, null]

labels:
   ### type can be `simple`, `custom`
   ### [option 1] use `simple` for binary/multi-class classification, then `value` is a list of 0-1 labels
   type: simple
   value: [ttHcc, ttHbb, ttLF, ttcc, ttbb]
   ### [option 2] otherwise use `custom` to define the label, then `value` is a map
   # type: custom
   # value:
   # target_mass: np.where(fj_isQCD, fj_genjet_sdmass, fj_gen_mass)

observers:
   - run
   - luminosityBlock
   - event
   - genWeight
   - xsecWeight
   - genTtbarId
   - genEventClassifier

weights:
   ### [option 1] use precomputed weights stored in the input files
   # use_precomputed_weights: true
   # weight_branches: [weight, class_weight]
   ### [option 2] compute weights on-the-fly using reweighting histograms
   use_precomputed_weights: false
   reweight_basewgt: genWeight*xsecWeight
   reweight_method: flat
   reweight_vars:
      lep1_eta: [-10000, 10000]
      lep1_phi: [-10000, 10000]
   reweight_classes: [ttHcc, ttHbb, ttLF, ttcc, ttbb]
   class_weights: [1, 1, 1, 1, 1]
   reweight_hists:
